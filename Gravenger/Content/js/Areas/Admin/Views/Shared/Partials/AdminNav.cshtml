<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #62228A;">
    <a class="navbar-brand" href="@Url.Content("~/")">Seenry</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Accounts
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    @if (User.IsInRole("Can Create Invitations"))
                    {
                        <a class="dropdown-item" href="@Url.Action("list", "invitation", new { area = "admin" })">Send an invitation</a>
                        <div class="dropdown-divider"></div>
                    }
                    @if (User.IsInRole("Can Manage Account Photos"))
                    {
                        <a class="dropdown-item" href="@Url.Action("index", "accountphoto", new { area = "admin" })">Manage Photos</a>
                        <div class="dropdown-divider"></div>
                    }
                    <a class="dropdown-item" href="#">Deactivate an account</a>
                </div>
            </li>
            @if (User.IsInRole("Can Manage Cards"))
            {
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("index", "card", new { area = "admin" })">Cards</a>
                </li>
            }
            @if (User.IsInRole("Can Manage Permissions"))
            {
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Permissions
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @if (User.IsInRole("Can Manage Account Permissions"))
                        {
                            <a class="dropdown-item" href="@Url.Action("ManageAccount", "permission", new { area = "admin" })">Account permissions</a>
                        }
                        <a class="dropdown-item" href="@Url.Action("ManageRoles", "permission", new { area = "admin" })">Add roles</a>
                    </div>
                </li>
            }
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    My Account
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">Update Account Information</a>
                    <a class="dropdown-item" href="@Url.Action("reset","account", new { area = string.Empty })">Change Password</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="@Url.Action("logout","account", new { area = string.Empty })">Sign Out</a>
                </div>
            </li>
        </ul>
    </div>
</nav>